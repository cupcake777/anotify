"""anotify — Cross-platform remote notification system for AI agents.

Send desktop notifications from remote servers (HPC, VPS, cloud) to your
local machine via a lightweight WebSocket relay.
"""

__version__: str = "0.1.0"
